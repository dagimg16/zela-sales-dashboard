services:
  - type: web
    name: zela-nails-dashboard
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: production
